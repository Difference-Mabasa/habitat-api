package com.habitat.api.enums;

/**
 * Mandate scope — what the agent is contracted to do for the
 * landlord. Mirrors backroom-api's MandateType.
 */
public enum MandateType {
    FULL_MANAGEMENT,
    TENANT_FIND,
    LETTING_AND_INSPECTIONS
}
